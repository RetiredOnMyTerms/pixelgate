# Python
.venv/
__pycache__/
*.pyc

# PyInstaller build artifacts (spec is kept as source)
bridge/build/
bridge/dist/
build/
dist/

# Node / Vite
web/node_modules/
web/dist/
node_modules/

# Logs & scratch
*.log

# Raw device screenshots (contain the real LocalToken/IP — only the redacted,
# annotated copies under web/public/onboarding/ are committed)
/image*.jpeg
/image*.jpg
/image*.png

# Secrets / local config (never commit)
.env
.env.*.local
.env.local
*.secrets.local
